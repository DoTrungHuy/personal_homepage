# This repository publishes a self-contained single-file homepage.
# Ignore every root entry unless it is explicitly part of the release set.
/*
!/.gitignore
!/README.md
!/index.html

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
